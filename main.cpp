#include <iostream>

int main() {
    int num1, num2, sum;

    // Input two numbers
    std::cout << "Enter two numbers: ";
    std::cin >> num1 >> num2;

    // Calculate sum
    sum = num1 + num2;

    // Output result
    std::cout << "Sum: " << sum << std::endl;

    return 0;
}
